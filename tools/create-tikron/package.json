{
  "name": "create-tikron",
  "version": "0.0.0",
  "private": true,
  "license": "FSL-1.1-ALv2",
  "type": "module",
  "description": "Scaffolder for Tikron games — clone the platform repo (or copy the starter template) and print the next steps.",
  "bin": {
    "create-tikron": "src/index.ts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ONOFFERDEV/tikron-platform.git"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2"
  }
}
