// Discord Activity configuration — loaded before the app bundle.
//
// Set this to your Discord Application (client) ID from the Developer Portal
// (Applications → your app → "Application ID"). It is NOT a secret — the client id
// is public — so it's fine to commit or paste here. Leave it empty to run the
// plain-browser dev fallback (open the page with ?dev=1).
globalThis.__DISCORD_CLIENT_ID__ = "";
